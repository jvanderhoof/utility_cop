<%= form_for(@app_credential) do |f| %>
  <%= f.hidden_field :app_id %>
  <% if @app_credential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_credential.errors.count, "error") %> prohibited this app_credential from being saved:</h2>

      <ul>
      <% @app_credential.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br>
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :text_value %>
  </div>
  <div class="actions">
    <%= f.submit submit_button_message(@app_credential) %>
  </div>
<% end %>

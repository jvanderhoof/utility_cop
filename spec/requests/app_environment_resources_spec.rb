require 'rails_helper'

RSpec.describe "AppEnvironmentResources", type: :request do
  describe "GET /app_environment_resources" do
    #it "works! (now write some real specs)" do
    #  get app_environment_resources_path(app_environment_id: create(:app_environment, app: build(:app), environment: build(:environment)).id)
    #  expect(response).to have_http_status(200)
    #end
  end
end
